* TODO
** DONE [#A] RMB should select before showing the context menu
   :DONE: set left_idx + focus before calling context_actions_popup() (app.py ~5982)
** DONE [#A] RMB in tab 0 is partly broken
   :DONE: RMB now checks queue_panel+mx>=left_w (miniqueue) and selects queue_cursor before popup, mirroring LMB logic (app.py ~5984)
** DONE [#C] fix o/O without hardcoding monochrome.tf
   :NOTE: subdomain-stripping approach commented out in web_base(); hardcoded to monochrome.tf for now with explanation (app.py ~821)
** DONE [#A] Error: urlopen error (errno -3) Temporary failure on name resolution: happened when scrolling songs; Samidy confirmed there is no rate limits on their APIs so the issue is probably just tuifi freaking out all by itself when too many actions are executed in a short time
** DONE [#C] artist page: consider an artist pic when hovering artist name
** DONE [#A] when changing the artist, the previous cover shows until a new album is hovered: we probably need to force an erase in that situation
   :DONE: erase + reset cover state at start of fetch_artist_async() when loading a new artist (app.py ~3698)
** DONE switching minic overs flickers in kitty; ueberzug to be tested with litekin user; I also observed some flicker on foot+chafa on one machine, not the other, despite similar configs.
** DONE [#A] clear filter and search prompts by default, add a setting to keep last input
   :DONE: all three prompts (search, filter, lyrics filter) start empty by default; set remember_last_input=true in settings.json to prefill with last query within a session (values never persisted across restarts)
** DONE [#B] when no song is in queue but one is played with Return, currently seeking backwards after the song ends does not play it again until p is pressed. Expected behaviour: song ends, seek backwards, play automatically from there
   :DONE: seek_rel with negative delta + dead mpv restarts _last_played_track from max(0, last_duration + delta); duration tracked live in _on_mpv_tick (app.py ~1593)
** DONE [#B] double check that the queue resumes position where it left off after it was interrupted by some priority (B) tracks, and not just at the position next to the priority track (i.e., it should remember what was the next non-priority track)
   :DONE: _queue_resume_idx saved when first priority added, restored in next_track() when priority_queue empties; remapped on track delete/insert; cleared on manual priority clear (app.py ~1625)
** DONE [#A] critical: when spamming Return on a track in a tab (tested Mix, others to be tested), mpv will play it a million times simultaneously and the history json file will also start throwing errors; we need to prevent snding actions faster than mpv can respond and handle them
   :DONE: _play_serial counter + _play_lock in play_track: stale threads bail after URL resolve and before/after mp.start(); only the last-requested play proceeds to record state (app.py ~1505)
** DONE [#A] Add Shift+up/down as alternatives to K/J (may not be terminal agnostic so tests needed)
   :DONE: KEY_SF (Shift+Down) and KEY_SR (Shift+Up) added via getattr() guards (app.py ~6620); needs cross-terminal testing
** DONE [#A] pressing .> or ,< skips to N+1/N-1 then immediately auto-advances again (plays N+2 or N)
   :DONE: auto-advance now guarded by _play_serial == _current_track_serial; any play_track call bumps _play_serial, suppressing the auto-advance for the entire bg-thread lifetime until the new track is confirmed (app.py ~5929)
** DONE [#A] RMB in queue tab does not visually select the track before showing the context menu
   :DONE: RMB handler now checks tab == TAB_QUEUE and updates queue_cursor/focus="queue" instead of left_idx (app.py ~6063)
** DONE When preview is enabled and we go to tab 2, the next song is selected instead of the current
   :DONE: switch_tab to TAB_QUEUE resets queue_cursor to queue_play_idx (app.py ~6334)
** DONE In tab 0, the lyrics paen should not reduce the main cover size
** DONE In tab 0, clicking on the main cover cycles view types, and when going back to miniqueue + minicover, it forgets about preview mode being enabled and just selects the current track
   :DONE: cover-click cycle to queue_overlay=True now re-applies preview_next cursor offset when _album_cover_pane is active (app.py ~6739)
** DONE When preview_next is enabled, add the text "Next in queue:" above the cover (same line as the tab names)
   :DONE: "Next in queue:" drawn at row 0 in the pane area when preview_next=True on TAB_PLAYBACK+queue+pane (draw() ~6184)
** Lyrics (see git.sr.ht #1) (not now, we'll see later)
** DONE When showing the help, the miniqueue jumps over the minocover (and/or spectrum), there's no need for that, it should stay where it is since we decided dialogs no longer freeze or hide status/info/side/cursor moving with queue next tracks.
   :DONE: removed hide_cover from _pane_suppressed/_cava_pane_active; gated only actual render calls; layout unchanged during popups (app.py ~6588)
** When the geometry is small and the sideview: both mode collapses to hide the spectrum, the miniqueue should shift up to use that space instead of staying where it is; there are 4 extra rows to use I believe
** DONE In the help dialog, there should be an empty row between the content (the help text) and the bottom line of the frame where the keybinds for the dialog are listed.
   :DONE: inner_h = box_h - 3 (was box_h - 2) in show_help_dialog() (app.py ~6390)
** DONE The 8s timer for hi_res_lossless is probably too long, makes dl very long when they have to fallback to lower quality, I think we know before 8 seconds if we found the first part for max quality
   :DONE: reduced DASH wait loop from range(16) to range(6) (~3s); log updated to "3s" (app.py ~1682)
** DONE Playback stops when a track ends and there are others in the queue if the download dialog is open, it should continue
   :DONE: download dialog ch==-1 branch now runs auto-advance check before continue (app.py show_download_queue_dialog)
** See if extra tags can be included, like lyrics, url, etc.
** See if we can avoid the cursor block being flickering/blinking at rows where redraws are occurring.
** DONE I don't see the spectrum color setting in setting.jsonc (I had the file before but would expect it to update), and there should also be a way to set the cava method (pulse, pipewire, etc) as well as the source.
   :DONE: color_spectrum/spectrum_method/spectrum_source added to persistence.py defaults and save_settings() SPECTRUM section; color_spectrum inherits color_chrome; sideview auto-detected on first run
**  Maybe make the help hint (help: ?) be more like " ? help " and ansi-escape reverse it all to make them look like a button, return it to the beginning of the toggle bar (so that users can hide it with the toggle bar), append the "  |  " separator again after it, not reversed, make it so the cursor block is always positioned at this line and at the column of the ? character (even after window resize), and position the info messages/toasts back to where they were in the previous commit (bottom right), and make them show in yellow (unless tuifi is set to black/white with the C key)
** I noticed the minicover in some conditions shifts slowly down when the spectrum is shown below it, without any user action. Happens with or without the miniqueue. Perhaps it's just the spectrum extending to it and it gets wrongly redrawn one row too low? Or some other cause. This happened to me in the Artist and Album tabs but maybe it's not specific to them. Maybe restricting the spectrum to fixed row numbers depending on the window geometry and preventing it from overextending would be enough?
** In the download dialog, DONE should be green, FAIL should be red, PEND should be chrome, CANC should be yellow, the whole dialog should be 10 cols wider to leave space for long track titles (maybe rewrap the keybind hints if there's enough space on the first line after that, don't forget that we have an extra space after the last to avoid touching the frame).
** There should be an extra set of keybinds for the download dialog: @: retry failed
** Clicking on the main cover in tab 0 should do the same cycle as V
** DONE The V cycle in tab 0 should not take marging/padding space on the maincover when the triple mode (maincover + lyrics + miniqueue/sideview) is enabled, but on the lyrics (they can be rewrapped to narrower width).
   :DONE: _cover_img_cols() uses max(45, _lyrics_panel_w(w)) as right_w in triple mode so cover stays same width as lyrics-only mode; _cic in draw() updated to use _cover_img_cols(w) instead of left_w-1 (app.py ~3790, ~7134)
** Download status at the bottom right should show in a line different from the info message because it can be long and will prevent other info messages/toasts to show while active. They should therefore be independent from the info/toast system (but they can re-use the functions partly of course). I suggest Download status stays where it is, i.e., one line above the bottom line, to the right, and info/toasts go back to the bottom right. This way, we don't waste space.
** Clicking on the download status info should open the download dialog.
** DONE See if we can easily include the cover into the downloaded files as tag
   :DONE: cover downloaded first (reused if exists), then passed to _tag_with_ffmpeg as -i input with -map 0:a -map 1:v -c:v copy -disposition:v attached_pic (app.py ~3183)
** DONE Got a crash, I don't know what triggered it as the PC was left unattended, tuifi was just downloading tracks:
   :DONE: drawing block in show_download_queue_dialog wrapped in try/except curses.error: break to handle terminal resize gracefully (app.py ~2944)
#+begin_src 
 e SystemExit(main(sys.argv))
                     ^^^^^^^^^^^^^^
  File "/home/mat/Projects/tuifi/tuifi_pkg/app.py", line 8717, in main
    debug_log(f"sixel probe error: {e}")
^^^^^^^^^^^^^^^^^^^^^^^
  File "/gnu/store/xkc78v2qj003v5vrvcdsqcwn0k2v3b6s-python-3.11.14/lib/python3.11/curses/__init__.py", line 94, in wrapper
    return func(stdscr, *args, **kwds)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/mat/Projects/tuifi/tuifi_pkg/app.py", line 8711, in wrapped
    os.close(fd)
^^^^^^^^^
  File "/home/mat/Projects/tuifi/tuifi_pkg/app.py", line 7503, in run
    # Format 1 — CSI u: "N;Mu"  (kitty/foot/CSI u protocol)
    ^^^^^
  File "/home/mat/Projects/tuifi/tuifi_pkg/app.py", line 2945, in show_download_queue_dialog
    win.erase()
    ^^^^^^^^^^^^
_curses.error: addwstr() returned ERR
⏎                                                                                                                                                                                                                                        ⏎mat@em780-guix~/P/tuifi $ 
#+end_src

HiFi API logs were also showing that, but not sure if related at all:
#+begin_src 
      127.0.0.1:58846 - "GET /track/?id=194068899&quality=LOSSLESS HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58850 - "GET /track/?id=194068899&quality=HIGH HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58864 - "GET /track/?id=194068899&quality=LOW HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58878 - "GET /trackManifests/?id=194068900&usage=PLAYBACK&manifestType=MPEG_DASH&uriScheme=HTTPS&adaptive=false&formats=FLAC_HIRES&formats=FLAC HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58890 - "GET /track/?id=194068900&quality=HI_RES_LOSSLESS HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58906 - "GET /trackManifests/?id=194068900&usage=PLAYBACK&manifestType=MPEG_DASH&uriScheme=HTTPS&adaptive=false&formats=FLAC HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58908 - "GET /track/?id=194068900&quality=LOSSLESS HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58922 - "GET /track/?id=194068900&quality=HIGH HTTP/1.1" 401 Unauthorized
INFO:     127.0.0.1:58938 - "GET /track/?id=194068900&quality=LOW HTTP/1.1" 401 Unauthorized
#+end_src

I don't know if related, but I might have been running out of disk storage while downloading (only 1GB left when I went back to the PC, with tuifi already crashed). Perhaps totally unrelated.
** DONE Maybe we could add a notification system when playing a new track, using notify-send, which would show the cover, artist, track, album. This sould be a toggle (notif: on) on key | (temporary but this one is free) and have a setting in toggles in settings.jsonc.
   :DONE: | toggles _notify_on_track; warns if notify-send absent; _send_track_notification fires in bg thread with cached cover icon; notify_on_track persisted in settings (app.py ~3067)
** DONE We need to add cover and lyrics to tracks tags when downloading, flac or m4a (depending on quality), and avoid redownloading the cover for each track when we can re-use them. We still download lyrics and cover files though, just add them as tags as well.
   :DONE: covered by the cover-in-tag item above; lyrics already embedded via _tag_with_ffmpeg since commit 431d7bf; cover reused from cover.jpg if already present
** DONE The prefetch system should maybe trigger before the last 5s of the next track, it would be best to actually prefetch as soon as the current track has buffered enough to avoid interruptions on slow connections. So maybe as soon as the current has 10s cache?
   :DONE: MPV.poll_once() now polls demuxer-cache-duration; main loop triggers prefetch when cache>=10s OR <=5s remain (audio.py poll_once, app.py main loop)
** DONE The prefetch system works for hi_res_lossless and lossless qualities, but it will cause errors for all other qualities (that do not use dash) and prevent playing new tracks (be it with autoadvance or track skipping).
   :DONE: _prefetch_next_url() now returns early for non-DASH qualities (HIGH/LOW) to avoid short-lived URL caching and thread-safety races on _last_url_is_manifest (app.py ~1925)
** DONE When pressing prev-track while a priority track is being played, the prev track should actually be the last one played previously, not the prev track adjacent to the priority one.
   :DONE: skip debounce delta<0 now checks _queue_resume_idx; if set (priority active), plays _queue_resume_idx instead of queue_play_idx-1 (app.py skip debounce)
** I still see some references to autoplay in the code or functions but I think we wanted to rename it all to autoextend.
** Make sure the spectrum never overextends and alters the minicover. It should have reserved space and not expand beyond (maybe it is the case already, but do make sure).
** DONE In tab 0, when in landscape, everytime the lyrics are shown (with V or with clicking on the main cover to cycle modes), this pushes the maincover to the left a little bit and decrease its size. In fact, the main cover should use the maximal space and the lyrics should appear in the not used space, with padding/margin between maincover and lyrics, but without touching the maincover. And when the miniqueue is shown too (triple mode), then this padding should be reduced a bit (but not suppressed) by moving lyrics to the left a little bit, ad making them narrower (to leave space for miniqueue), but the maincover should remain untouched. Curently the cover gets pushed to the left (and therefore decreased in height too) whenever lyrics show. And when in portrait or square geometry, the lyrics should go below the maincover, and the miniqueue, when shown, should decrease the maincover width accordingly to get space for itself. Currently, in this square/portrait layout, the maincover overexpands over the miniqueue and minicover; this did not use to be the case: it was working in the commits before latest changes.
   :DONE: _cover_img_cols() now always returns height-based natural square (rows×2) on TAB_PLAYBACK landscape, stable across all layout modes; lyrics at cic+1 gap; triple mode takes space from lyrics not cover (app.py ~3775, ~7128)
** In the help, I see this:
#+begin_src 
            " playback_tab_layout: default layout in tab 0",
            "   values: \"lyrics\" (default), \"miniqueue\", \"miniqueue_cover\"",
            " playback_tab_preview_next: sideview shows next track cover on tab 0 (toggle with n)",
            "   (requires miniqueue + minicover pane; default: false)",
#+end_src
In fact, there should be a layout setting for triple mode too, right? i.e., miniqueue_lyrics and miniqueue_lyrics_cover
Also, this should be the default.
Also, preview_next should be default too, since we don't really need twice the same cover in the playback tab and we made miniqueue_cover_lyrics default.
** When a dialog is open (such as help), autoadvance should still work.
