* TODO
** DONE [#A] RMB should select before showing the context menu
   :DONE: set left_idx + focus before calling context_actions_popup() (app.py ~5982)
** [#A] RMB in tab 0 is partly broken
** [#C] fix o/O without hardcoding monochrome.tf
** [#A] Error: urlopen error (errno -3) Temporary failure on name resolution: happened when scrolling songs; Samidy confirmed there is no rate limits on their APIs so the issue is probably just tuifi freaking out all by itself when too many actions are executed in a short time
** [#C] artist page: consider an artist pic when hovering artist name
** DONE [#A] when changing the artist, the previous cover shows until a new album is hovered: we probably need to force an erase in that situation
   :DONE: erase + reset cover state at start of fetch_artist_async() when loading a new artist (app.py ~3698)
** switching minicovers flickers in kitty; ueberzug to be tested with litekin user; I also observed some flicker on foot+chafa on one machine, not the other, despite similar configs.
** DONE [#A] clear filter and search prompts by default, add a setting to keep last input
   :DONE: prompt_text() now called with "" in filter_prompt() and lyrics_filter_prompt() (app.py ~4477, ~4506); "keep last input" setting not yet added
** [#B] when no song is in queue but one is played with Return, currently seeking backwards after the song ends does not play it again until p is pressed. Expected behaviour: song ends, seek backwards, play automatically from there
** [#B] double check that the queue resumes position where it left off after it was interrupted by some priority (B) tracks, and not just at the position next to the priority track (i.e., it should remember what was the next non-priority track)
** [#A] critical: when spamming Return on a track in a tab (tested Mix, others to be tested), mpv will play it a million times simultaneously and the history json file will also start throwing errors; we need to prevent snding actions faster than mpv can respond and handle them
** DONE [#A] Add Shift+up/down as alternatives to K/J (may not be terminal agnostic so tests needed)
   :DONE: KEY_SF (Shift+Down) and KEY_SR (Shift+Up) added via getattr() guards (app.py ~6620); needs cross-terminal testing
